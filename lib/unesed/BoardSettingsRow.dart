import 'package:flutter/material.dart';

class BoardSettingsRow extends StatelessWidget {
  const BoardSettingsRow({
    Key key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Row(
      children: [
        Container(
          color: Colors.teal,
        )
      ],
    );
  }
}
